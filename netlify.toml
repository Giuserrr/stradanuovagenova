[build]
  command = "npm install && npm run build"
  publish = "."
  functions = "netlify/functions"

[functions]
  node_bundler = "nft"

# Catch-all SPA rimosso in Fase 6 (commit con conversione /chi-siamo /appuntamento
# /grazie /contatti a file fisici). Netlify serve i file fisici e ricade su
# 404.html per i path inesistenti.
