#!/usr/bin/env node
// build-sitemap.js — stub Fase 0
// Implementazione completa in Fase 2: scansiona *.html, esclude admin/404/noindex,
// produce sitemap.xml con <loc> + <lastmod> da git log -1 --format=%cI <file>.
// Niente priority/changefreq (ignorati da Google nel 2026).
console.log('[build-sitemap] stub — implementazione in Fase 2');
