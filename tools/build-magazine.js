#!/usr/bin/env node
// build-magazine.js — stub Fase 0
// Implementazione completa in Fase 12: scansiona magazine/content/*.md,
// parsing YAML frontmatter con gray-matter, body Markdown con marked,
// output magazine/articles/{slug}/index.html con JSON-LD Article.
console.log('[build-magazine] stub — implementazione in Fase 12');
